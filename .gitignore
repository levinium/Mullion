bin/
obj/
.vs/
.idea/
*.user
*.suo
TestResults/
artifacts/
publish/
*.log

# Regenerated by tools/Capture-Simulations.ps1. Rooted, so it covers the
# scratch directory that script writes to and NOT docs/screenshots/, which is
# the handful of shots the README points at and has to be in the repository.
/screenshots/
